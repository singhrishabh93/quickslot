import 'package:get_it/get_it.dart';

/// No cubits to register — SduiScreenCubit is constructed per page via
/// BlocProvider with the path as a parameter, not from get_it.
void setupCubitLocator(GetIt getIt) {}
